services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: mimin
      POSTGRES_PASSWORD: mimin
      POSTGRES_DB: mimin
    ports:
      - "5432:5432"
    volumes:
      - mimin-postgres:/var/lib/postgresql/data
volumes:
  mimin-postgres:
